{
	"icons": {
		{{#each codepoints}}
		"trilogy-{{@key}}": {
			"description": "{{@key}} icon",
			"default": {
				"fontPath": "dist/trilogyicons.woff2",
				"fontCharacter": "\\\{{codepoint this}}"
			}
		{{#if @last}}
		}
		{{else}}
		},
		{{/if}}
		{{/each}}
	}
}